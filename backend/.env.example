# Railway 会自动设置这些环境变量
# DATABASE_URL - PostgreSQL 连接字符串（由 Railway 自动提供）
# PORT - 服务器端口（由 Railway 自动提供，通常是 8000）

# 本地开发时可使用 SQLite
# DATABASE_URL=sqlite:///./supplier.db

# 如果需要连接到 Railway 部署的后端
# VITE_API_URL=https://your-railway-app.railway.app
